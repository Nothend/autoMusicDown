# 虚拟环境 / 版本控制 / CI（不进构建上下文，加快构建）
venv/
.venv/
env/
.git/
.github/

# 本地日志和下载文件（通过volume映射，无需打包）
logs/
downloads/
.vscode/

# 其他临时文件
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store
# 排除根目录的config.yaml
config.yaml